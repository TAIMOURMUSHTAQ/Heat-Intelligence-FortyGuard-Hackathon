services:
  - type: web
    name: heat-intelligence
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir src api:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health